name: Build and Deploy
on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
#    concurrency: ci-${{ github.ref }} # Recommended if you intend to make multiple deployments in quick succession.
    runs-on: ubuntu-latest
    env:
      BUILD_ID: build-${{ github.run_id }}-${{ github.run_attempt }}
      DIST_PATH: /

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - id: packagejson
        run: |
          contents=$(cat ./package.json)
          echo "::set-output name=contents::$contents"

      - name: Print id
        run: echo ${{ env.BUILD_ID }}

      - name: Install and Build 🔧 # This example project is built using npm and outputs the result to the 'build' folder. Replace with the commands required to build your project, or remove this step entirely if your site is pre-built.
        run: |
          npm ci
          npm run build

      - name: Update dist path with version
        run: |
          cat package.json
          echo ${{ fromJson(steps.packagejson.outputs.contents).version }}
          echo DIST_PATH=/v/$(echo 'elo' )

#      - name: Deploy 🚀
#        uses: JamesIves/github-pages-deploy-action@v4
#        with:
#          token: ${{ secrets.GITHUB_TOKEN }}
#          folder: dist # The folder the action should deploy.
#          target-folder: v/${{ env.BUILD_ID }}
#          CLEAN: true # Automatically remove deleted files from the deploy branch
#          CLEAN_EXCLUDE: [ 'v' ]

